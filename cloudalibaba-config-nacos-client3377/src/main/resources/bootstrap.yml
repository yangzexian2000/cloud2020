server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #服务注册中心地址
      config:
        server-addr: localhost:8848 #配置中心地址
        file-extension: yaml #指定yaml格式的配置
#        group: DEV_GROUP 加组
        namespace: 2bd87fa4-1a27-4b6a-b4f1-fbe9310b0976 #这是加上命名空间 namespace的id
